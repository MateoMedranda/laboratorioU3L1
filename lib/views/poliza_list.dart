import 'package:flutter/material.dart';

class PolizaList extends StatelessWidget {
  const PolizaList({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Listado de Pólizas'));
  }
}
