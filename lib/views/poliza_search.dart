import 'package:flutter/material.dart';

class PolizaSearch extends StatelessWidget {
  const PolizaSearch({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Buscar Póliza'));
  }
}
